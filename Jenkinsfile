pipeline {
    agent any
    stages {
        stage('Print hello') {
            steps {
                echo "JENKINS_HOME=$JENKINS_HOME"
                echo 'Hello world!'
            }
        }
    }
}